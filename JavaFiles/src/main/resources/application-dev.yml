jwt:
  secret: A^DJAI@(FHQIS(FHQ*#&*!KK

server:
  error:
    path: /user/error
    whitelabel:
      enabled: false

Spring:
  datasource:
    url: jdbc:mysql://localhost:3306/safebusiness
    password:
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        globally_quoted_indentifiers: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  sql:
    init:
      mode: never
      continue-on-error: true

spring:
  mail:
    host: smtp.gmail.com
    default-encoding: utf-8
    port: 465
    username: songweifengmarcus@gmail.com
    password: qiycuqyhgzhxxdxd
    protocol: smtp
    properties:
      mail:
        smtp:
          writetimeout: 5000
          connectiontimeout: 5000
          timeout: 5000
          auth: true
          starttls:
            enable: true
            required: true
        ssl:
          enable: true